import { pageTitle } from "@/lib/metadata";

export const metadata = pageTitle("Track Order");

export default function TrackLayout({ children }: { children: React.ReactNode }) {
  return children;
}
